---
import type { ArtworkTone } from '../data/maimai';

interface Props {
	title: string;
	tone: ArtworkTone;
	compact?: boolean;
}

const { title, tone, compact = false } = Astro.props;
const initial = title.trim().slice(0, 1).toUpperCase();
---

<span class:list={['song-artwork', `tone-${tone}`, { compact }]} aria-hidden="true">
	<span>{initial}</span>
</span>
